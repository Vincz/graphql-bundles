parameters:

services:
    Overblog\GraphQL\Bundle\ConfigurationGraphQLBundle\Generator\ConfigurationGraphQLParser:
        arguments:
            - ["%kernel.project_dir%/config/configuration/mapping"]
            - "%kernel.project_dir%"
            - "%kernel.bundles%"
        tags:
            - { name: "overblog_graphql.configuration_generator" }